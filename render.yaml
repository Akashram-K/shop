services:
  - type: web
    name: royal-jewellery
    env: docker
    plan: free
    region: singapore
    branch: master
    dockerfilePath: Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 8080
